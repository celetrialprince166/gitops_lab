# =============================================================================
# Environment Variables Template - Notes Application
# =============================================================================
# Purpose: Template for required environment variables
# Security: NEVER put real credentials here - use AWS Secrets Manager in production
# Usage: Copy this file to .env and fill in with your actual values (locally only)
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL credentials (fetch from AWS Secrets Manager in production)
DB_USERNAME=dbadmin
DB_PASSWORD=changeme_use_strong_password
DB_NAME=notesdb
DB_SSL=false

# =============================================================================
# PROXY CONFIGURATION
# =============================================================================
# Port for Nginx reverse proxy
PROXY_PORT=80

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Public API URL (build-time variable for Next.js)
# Local: http://localhost/api
# Production: http://your-ec2-public-ip/api or https://yourdomain.com/api
NEXT_PUBLIC_API_URL=http://localhost/api

# =============================================================================
# AWS CONFIGURATION
# =============================================================================
# AWS region — REQUIRED for the awslogs Docker log driver to stream container
# logs to CloudWatch. Must match the region where your EC2 instance runs.
AWS_REGION=eu-west-1

# AWS profile (if using named profiles locally)
# AWS_PROFILE=default

# =============================================================================
# TERRAFORM CONFIGURATION (Optional)
# =============================================================================
# Environment name (dev, staging, prod)
# TF_VAR_environment=dev

# EC2 instance type
# TF_VAR_instance_type=t3.small

# AWS region
# TF_VAR_aws_region=eu-west-1

# =============================================================================
# NOTES
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Update .env with your actual values
# 3. NEVER commit .env to Git (it's in .gitignore)
# 4. In production, secrets are fetched from AWS Secrets Manager
# 5. CI/CD uses GitHub OIDC for AWS authentication (no credentials needed)
